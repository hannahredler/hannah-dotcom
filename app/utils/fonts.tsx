import { PT_Mono } from 'next/font/google'
import { Poppins } from 'next/font/google'

export const fontMono = PT_Mono({ weight: '400', subsets: ['latin'] })
export const fontPoppins = Poppins({ weight: '500', subsets: ['latin'] })
